version: 2

# The Next.js app is a scaffold example, not a production app shipped by this repo.
# Keep CI npm audit coverage, but prevent Socket from treating example framework
# capabilities as repo-level production supply-chain risk.
projectIgnorePaths:
  - "examples/nextjs-webapp/package.json"
  - "examples/nextjs-webapp/package-lock.json"
