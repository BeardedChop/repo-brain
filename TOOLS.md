# TOOLS.md

tool-specific notes, environment setup, and API references.
this is where you put the stuff that's unique to YOUR setup, not the project.

## environment

- node version:
- package manager:
- OS:
- any env vars needed:

## api references

if this project uses external APIs, put the docs links here.
don't paste full API docs — the AI can read the links.

- api name: https://docs.example.com
- api name: https://docs.example.com

## tool config

anything specific to your dev setup.

- linter config:
- formatter:
- test runner:
- deployment target:

## common commands

```bash
# start dev server
npm run dev

# run tests
npm test

# build for production
npm run build

# deploy
npm run deploy
```

## known issues

things that are broken, workarounds, stuff the AI should know about.

- issue: description → workaround
- issue: description → workaround

## local secrets

reference where secrets live. NEVER put actual values here.

- `DATABASE_URL` → `.env.local`
- `API_KEY` → `.env.local`
- see `.env.example` for required variables
